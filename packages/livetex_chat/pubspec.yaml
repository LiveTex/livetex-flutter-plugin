name: livetex_chat
description: Pure Dart LiveTex chat core over Visitor API (auth, WS, state, reconnect).
version: 0.2.0
publish_to: "none"

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  http: ^1.2.2
  web_socket_channel: ^3.0.1

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
